name: Build and Sign Release

on:
  release:
    types: [published]
  # Allows you to trigger the workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  build-and-sign:
    name: Build, Sign, and Release
    runs-on: windows-latest
    permissions:
      contents: write
    
    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Setup Rust Toolchain
      uses: dtolnay/rust-toolchain@stable
      with:
        target: x86_64-pc-windows-msvc

    - name: Build Release
      run: cargo build --release
      
    - name: Rename Executable for Clarity
      run: mv target/release/rSMPP.exe rSMPP.exe

    # - name: Submit to SignPath
    #   uses: signpath/github-action-submit-signing-request@v1
    #   with:
    #     api-token: '${{ secrets.SIGNPATH_API_TOKEN }}'
    #     organization-id: '${{ secrets.SIGNPATH_ORGANIZATION_ID }}'
    #     project-slug: '${{ secrets.SIGNPATH_PROJECT_SLUG }}'
    #     signing-policy-slug: '${{ secrets.SIGNPATH_SIGNING_POLICY_SLUG }}'
    #     github-artifact-name: 'rSMPP-unsigned'
    #     wait-for-completion: true
    #     output-artifact-directory: 'signed-artifacts'
    #     artifact-configuration: |
    #       artifacts:
    #       - file: rSMPP.exe
    # 
    # - name: Upload Signed Artifact to GitHub Release
    #   # Only upload if this was triggered by a release
    #   if: github.event_name == 'release'
    #   uses: softprops/action-gh-release@v2
    #   with:
    #     files: signed-artifacts/rSMPP.exe

    - name: Upload Artifact to GitHub Release
      # Only upload if this was triggered by a release
      if: github.event_name == 'release'
      uses: softprops/action-gh-release@v2
      with:
        files: rSMPP.exe
